# ============================================
# DEFAULT PROFILE (DEVELOPMENT)
# ============================================
spring.application.name=ai

# Active profile (dev atau prod)
spring.profiles.active=prod

# Debug mode (development atau production)
debug.mode=production

# Server
server.port=8080